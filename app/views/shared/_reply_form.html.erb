<%= form_for(comment) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
   <div>
     <%= f.text_area :content %>
   </div>
    <button type="submit">
      Comment
    </button>
 <% end %>